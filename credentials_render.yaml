host: localhost

postgres:
  user: {{ POSTGRES_USER }}
  password: {{ POSTGRES_PASSWORD }}
  schema: {{ POSTGRES_USER }}
  port: {{ POSTGRES_PROD_PORT }}

clickhouse:
  user: {{ CLICKHOUSE_USER }}
  password: {{ CLICKHOUSE_PASSWORD }}
  schema: {{ CLICKHOUSE_USER }}
  port: {{ CLICKHOUSE_PORT_1 }}

mlflow:
  uri: 172.18.0.3:5000

storage_s3:
  user: {{ MINIO_ROOT_USER }}
  password: {{ MINIO_ROOT_PASSWORD }}

airflow:
  user: {{ AIRFLOW_USER }}
  password: {{ AIRFLOW_PASSWORD }}
